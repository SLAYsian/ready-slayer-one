<div class="text-center">
  <main>
    <div id="loading-message" style="display: none;">Initializing quest...</div>

    <section id="chat-section">
      {{#each chats}}
        <p class="{{role}} chat-message">
          <span><b>{{role}}</b></span>
          <span>:</span>
          <span>{{content}}</span>
        </p>
      {{/each}}
    </section>

    <div id="typing-indicator" class="hide">
      <p><i>{{#if isTyping}}Typing{{/if}}</i></p>
    </div>

    <form id="chat-form">
      <input
        type="text"
        id="message-input"
        name="message"
        value="{{message}}"
        placeholder="Type a message here and hit Enter..."
      />
    </form>
  </main>
</div>

<script src="./js/chat.js"></script>

