<div class="row">
  <div class="col-auto">
    <h2>Welcome, {{name}}!</h2>
  </div>
</div>

<div class="row mt-4">
  <div class="col-md-6">
    <h3>All your adventures:</h3>
    {{#each userOutcomes}}
      <div>
        <h4>{{this.name}}</h4>
        <p>{{this.description}}</p>
      </div>
    {{/each}}
  </div>
</div>

<script src="./js/profile.js"></script>
